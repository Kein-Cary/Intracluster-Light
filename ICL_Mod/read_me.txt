1. In the pipe. calculation, pixel resampling use 'resample_module.py'
2. The 'SEX' path save the parameters setting for source finding.
3. make sure all the path in code is correct before applying.
4. 'ICL_sky_img_stack.py' include random center stacking case, the part 
need to run more than once and then take the mean img.
        
